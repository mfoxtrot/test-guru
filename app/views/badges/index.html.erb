<h1><%= t('.header') %>:</h1>

<%= render 'shared/errors', resource: @badge %>

<table class="table table-hover">
  <thead class="thead">
    <tr>
      <th>
        <%= t('.name') %>
      </th>
      <th><%= t('.rule_name') %></th>
      <th><%= t('.rule_parameter') %></th>
      <th><%= t('.image') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @badges %>
  </tbody>
</table>
